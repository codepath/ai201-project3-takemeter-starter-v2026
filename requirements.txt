# AI201 pair 3 — pinned with BOTH lower and upper bounds, on purpose.
#
# These are for the work on YOUR MACHINE: the baseline, and reading your data.
# The training runs in Colab, which installs its own copies — see the first
# cell of takemeter.ipynb.
#
# Supported Python: 3.11, 3.12, or 3.13. NOT 3.14.

# Reading and counting your labelled data
pandas>=2.0,<3.0

# The zero-shot baseline (unit 6) and the fine-tune (unit 5). Both run on your
# own machine — no API key, no account.
transformers>=4.44,<5.0

# What the training notebook needs on top of transformers. Colab used to
# install these itself; the notebook now runs here, so they're pinned with
# everything else.
datasets>=2.20,<4.0
accelerate>=0.30,<2.0

# transformers needs a backend. CPU-only is fine: this dataset is small enough
# that a full three-seed run is minutes. If your machine has an NVIDIA GPU or
# Apple Silicon, the notebook finds and uses it on its own.
torch>=2.2,<3.0

# Scoring the baseline
scikit-learn>=1.3,<2.0
